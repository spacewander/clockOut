<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= @title or 'clockOut' %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= yield :javascript %>
  <%= csrf_meta_tags %>
</head>
<%= render_body_tag %>
  <%#在用户登录后显示导航条%>
  <%= yield :navbar %>
  <div class="container-fluid" id="main">
    <div class="row">
      <div class="clockOut-panel col-xs-12 col-md-8 col-md-offset-2" id="panel">
        <%= yield %>
      </div>
      <div class="col-xs-12 col-md-2 sidebar">
        <%#对于用户正在监督的打卡和参加的活动，显示在右侧的边框中，大概占页面六分之一%>
        <%= yield :sidebar %>
      </div>
    </div>
  </div>
</body>
</html>

